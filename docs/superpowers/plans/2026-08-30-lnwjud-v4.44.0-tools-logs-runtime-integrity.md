# lnwjud v4.44.0 Tools, Lossless Logs, and Runtime Integrity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILLS: Use `subagent-driven-development` to implement this plan task-by-task, `test-driven-development` for every behavior change, `systematic-debugging` for unexpected failures, `impeccable` for the Tools/Logs UI, and `verification-before-completion` before any completion claim. The user explicitly authorized delegated implementation, scoped commits, one push to `dev`, and a local Windows installer build. Do not tag, publish, create a GitHub Release, or install the build for the user.

**Status:** Authorized for execution on `dev`. Source baseline was fast-forwarded to `origin/main` at `21bcbbf` before implementation; `dev` is currently one commit ahead of `origin/dev` for that fast-forward.

**Goal:** ออกชุดทดสอบภายในเครื่องสำหรับ lnwjud `4.44.0` ที่ (1) เก็บและส่งออกชื่อไฟล์/ชื่อเครื่องมือของแต่ละ log event ครบทุกตัวโดยไม่ใช้ `(+N)`, (2) ย่อเฉพาะการแสดงผลแต่ละแถวและกด “ดูเพิ่ม/แสดงน้อยลง” ได้, (3) แสดงสถานะ Tools อย่างตรงความจริงและอ่านง่าย, (4) แยก Browser “ต้องเปิดใช้งาน” ออกจาก “ต้องตั้งค่า”, (5) ทำ Accessibility / Computer Use / UI Target Action ให้ผ่าน integrity ของ packaged runtime จริง, และ (6) ทำ `agent_swarm_run` เป็น Codex-backed read-only tool ที่ opt-in, จำกัดขอบเขต/จำนวน, เป็นเจ้าของงาน และยกเลิกงานที่ยังมี verified runtime handle ได้

**Architecture:** Activity Tracker จะเก็บ `targetSummary` แบบสั้นควบคู่กับ full detail ที่ผ่าน shared redaction และ persist ครั้งเดียวใน SQLite. Work Log/Live Log rows ส่งเพียง immutable detail reference, item count, preview และ legacy flag; renderer lazy-load ทีละรายการ ส่วน copy/export/query resolve และ stream ข้อมูลเต็มจาก main process โดยไม่อิง DOM ที่ย่ออยู่. Tool Catalog จะเพิ่ม reason/delivery state แยกจาก readiness หยาบเพื่อไม่ตีความ runtime ที่ปิดว่าไม่ได้ setup. Windows capability bridge จะถูก stage เป็น byte artifact เดียวก่อน hash/build/package และ installer verifier จะพิสูจน์ source = staged = manifest = app expectation = packaged bytes พร้อมเรียก probe จาก packaged runtime จริง. Agent Swarm จะเป็น deep module read-only แยก มี durable ledger/terminal result, Codex quota opt-in, trusted approval, enforced read-only sandbox และ per-swarm concurrency; active child ที่ข้าม app restart จะรายงาน `termination_unverified` โดยไม่อ้างว่ายกเลิกหรือกู้ handle ได้.

**Tech Stack:** TypeScript, React, Electron, Node.js 24, SQLite WAL, MCP, PowerShell capability bridge, electron-builder, Vitest, Playwright, pnpm `10.15.0`

## Global Constraints

- ทุกชื่อใน collection ของ `read_many_files` และ `tool_batch` ต้องถูกเก็บหลัง redaction ครบตาม schema limit และลำดับเดิม; ห้ามใช้ `(+1)`, `(+2)`, `(+N)` กับ event ใหม่
- การย่อเป็น presentation-only ต่อรายการ: ปุ่มต้องมี `aria-expanded`, `aria-controls`, focus ring และข้อความไทย/อังกฤษ; เมื่อขยายต้องเห็นทุก item ของ event นั้น
- Copy และ export ต้องใช้ full structured model เสมอ ไม่ว่ารายการจะย่อหรือขยายอยู่
- ห้ามแนบ full item arrays กับ dashboard rows หรือ LogHub buffer ทุกแถว; row contract ใช้ `{ detailRef, itemCount, preview, legacyIncomplete }` และ lazy-load ได้ครั้งละหนึ่งรายการ
- SQLite เป็น full-detail authority เพียงแห่งเดียว; append-only `mcp-activity.log` เก็บ summary/reference เท่านั้นและห้ามทำสำเนา array ขนาดใหญ่
- Work Log identity ต้องแยก persisted `audit:<eventId>` กับ `inflight:<callId>`; export จับ immutable identity list ตอนคลิก และถ้า call จบระหว่าง export ให้ resolve started audit event ด้วย `callId` โดยไม่ทำรายการหาย
- Query/search ต้องทำใน main process และค้นชื่อ item ที่ซ่อนอยู่ได้โดยคืนเฉพาะ matching row IDs; ห้าม hydrate full detail ทุกแถวเพื่อค้นหา
- ข้อจำกัด retention ของหน้าจอ (จำนวน event ที่เก็บ) เป็นคนละเรื่องกับความครบของ item ภายใน event; งานนี้รับประกัน **all items per retained event**
- Log เก่าที่ถูกบันทึกเป็น `(+N)` ไปแล้วกู้ชื่อที่หายไม่ได้ ต้องแสดงคำเตือน “รายละเอียดเดิมไม่ครบจากเวอร์ชันก่อน” และห้ามสร้างชื่อสมมติ
- Redaction, bounded IPC, max event count และ file-size safety ต้องยังอยู่; แก้ truncation ด้วย structured bounded fields ไม่ใช่ปล่อย string ไม่จำกัด
- `needs_setup` ใช้เมื่อผู้ใช้ต้องติดตั้ง/ตั้งค่าจริงเท่านั้น; Managed Browser ที่ติดตั้งแล้วแต่ปิดอยู่ต้องเป็น `runtime_not_ready` พร้อม CTA “เปิด Managed Browser”
- `unknown` ใช้เฉพาะ probe timeout/crash/invalid response/integrity uncertainty; ห้ามใช้แทน planned, disabled, unsupported หรือ runtime stopped
- สี selected state ต้องผ่าน WCAG AA สำหรับตัวเลขและ label, เห็น focus ชัด และไม่พึ่งสีอย่างเดียว
- ห้ามลดความปลอดภัยด้วยการข้าม hash, เชื่อ manifest ที่แก้ได้ขณะ runtime หรือปิด PowerShell bridge integrity check
- `agent_swarm_run` ต้องไม่ปรากฏใน `tools/list` เมื่อ `codexToolsEnabled=false`; ทุก `start`/`cancel` ต้องผ่าน trusted host approval และ mutation policy
- Agent Swarm v4.44.0 รองรับ 1–4 read-only agents, default concurrency 2; reject `workspace-write` และ path-level write claims ที่ enforce ไม่ได้
- ห้าม auto-retry หรือสร้าง agent เพิ่มนอก request; partial launch ต้องหยุด children ที่เริ่มโดย swarm นั้นและรายงาน outcome รายตัว
- Swarm ledger ห้ามเก็บ prompt plaintext; เก็บ owner, digest/length, runtime handle identity, durable output/result location, timestamps และ terminal/cancellation state เท่านั้น
- หลัง restart terminal receipt/output ที่ persist แล้วอ่านได้ตาม owner; active child ที่ไม่มี verified live handle ให้สถานะ `termination_unverified` และห้ามอ้างว่า cancelled/completed/reattached
- ใช้ test-first: ต้องเห็น focused RED จาก behavior ใหม่ก่อนแก้ production และเห็น GREEN หลังแก้
- รักษา user-owned changes; stage/commit เฉพาะไฟล์ของแผนนี้ และห้าม reset/stash/broad format
- ปลายทาง Git คือ `dev` เท่านั้น; build ด้วย `--publish never`; ห้าม tag/publish/release

---

## Current Evidence and Root Causes

| Area | Verified current behavior | Root cause / decision |
| --- | --- | --- |
| `read_many_files` / `tool_batch` | UI และ exported Work Log มี `(+4)`, `(+3)`, `(+1)` | `activity-tracker.ts` ตัดเหลือ 3 paths หรือ 4 calls ก่อน persistence; ต้องย้ายการย่อไป renderer และเก็บ structured full detail |
| Long summaries | `summarizeForLog` ตัดที่ 4,096 chars | string summary ไม่ใช่ lossless transport; persist bounded detail ครั้งเดียวและให้ row ใช้ locator/count/preview |
| Work Log export | renderer ส่ง rows ที่จำกัด 16,384 chars ต่อแถว | export ต้องเลือก event IDs/scope แล้ว format จาก authoritative main-process model |
| Live Logs export | main process re-reads selected line IDs | seam ถูกทางแล้ว แต่ `LogLine` ต้องพก structured detailและ formatter ต้องขยายครบ |
| Selected status color | label ของ READY/NEEDS_SETUP ถูก selector `.tool-status-strip span` เปลี่ยนเป็น muted บนพื้นทอง | selected label ต้อง inherit foreground และมี regression contrast/focus test |
| Browser Debug Context | เมื่อ Managed Chrome ปิด probe ตอบ `available=true, ready=false` แต่ catalog แปลงเป็น `needs_setup` | เพิ่ม `readinessReason=runtime_not_ready` และ CTA เปิด browser; persistence ของ setup ไม่ได้หาย |
| Accessibility / Computer Use / UI Target Action | ทั้งสาม share `windows_ui_automation`; installed 4.31.0 เคยแสดง UNKNOWN จาก hash uncertainty | stage/hash/package bridge bytes จาก artifact เดียวและตรวจ installer จริง; runtime probe ต้องชี้ requirement ย่อยที่ล้มเหลว |
| Capability bridge packaging | build hash source แล้ว electron-builder copy source ซ้ำภายหลัง; release evidence ไม่รวม bridge | มี TOCTOU gap แม้ checkout ปัจจุบันจะ byte-consistent; package staged artifact เท่านั้นและ fail release evidence เมื่อ identity ไม่ตรง |
| `agent_swarm_run` | catalog ระบุ planned และ runtime คืน unavailable | การเปิดดื้อ ๆ จะข้าม Codex opt-in/policy และไม่มี enforced sandbox/concurrency; v4.44.0 เปิดเฉพาะ bounded read-only mode พร้อม owner/cancel/result ledger |
| Baseline MCP test | `sandbox_exec produces its declared runtime evidence` fail เฉพาะเครื่องที่มี Windows Sandbox เพราะ fixtureไปชน runtime จริง | แยก deterministic fixture จาก host-dependent availability; อย่าซ่อน genuine runtime failure |

Baseline focused gates before source changes:

```powershell
corepack pnpm@10.15.0 --filter @lnwjud/desktop exec vitest run --config vitest.config.ts tests/worklog-panel.test.ts tests/live-logs-layout.test.ts tests/tool-catalog-readiness.test.ts tests/tools-doctor-ui.test.ts
# 30 passed
corepack pnpm@10.15.0 --filter @lnwjud/capabilities exec vitest run src/windows-bridge-integrity.test.ts
# 5 passed
corepack pnpm@10.15.0 --filter @lnwjud/mcp-server exec vitest run src/activity-tracker.test.ts src/tool-runtime-contract.test.ts
# 282 passed, 1 host-dependent sandbox fixture failed
```

---

## Phase 1 — Lossless Per-Event Logs

### Task 1: Add structured activity target detail and persist it end-to-end

**Files:**

- Modify: `packages/audit/src/audit-types.ts`
- Modify: `packages/audit/src/audit-service.ts`
- Modify: `packages/audit/src/redactor.ts`
- Modify: `packages/storage/src/audit-repository.ts`
- Modify: `packages/mcp-server/src/activity-tracker.ts`
- Modify: `packages/mcp-server/src/activity-log-file.ts`
- Modify: `packages/mcp-server/src/server.ts`
- Modify: `apps/desktop/src/main/desktop-services.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `packages/ipc-contracts/src/index.ts`
- Modify: `apps/desktop/src/main/log-hub.ts`
- Modify: `apps/desktop/src/preload/index.ts`
- Test: `packages/mcp-server/src/activity-tracker.test.ts`
- Test: `packages/mcp-server/src/activity-log-file.test.ts`
- Test: `apps/desktop/tests/worklog-panel.test.ts`
- Test: `apps/desktop/tests/live-log-correlation.test.ts`

**RED:** Add fixtures with 7 file paths, 9 batched tools, duplicates, Unicode and redacted sensitive fragments. Assert the started audit record can resolve every sanitized item in original order, completed/JSONL/row records contain only the compact reference, and no new record contains a `(+N)` marker. Assert 500 maximum-length paths do not inflate a 500-row dashboard snapshot and legacy events without detail still decode.

**Implementation:** Introduce a bounded `ActivityTargetDetail` union (`files | tools`) and a `describeToolTarget` result containing short `summary` plus full detail. Use the audit package redactor on every item before any sink. Persist full detail only on the started audit event and resolve it by immutable event ID/call ID; `mcp-activity.log`, completed events, `buildWorkLog`, activity sinks and LogHub retain only `{detailRef,itemCount,preview,legacyIncomplete}`. Add repository projections that do not deserialize large metadata for dashboard rows and a bounded single-detail lookup/search-ID query.

**GREEN:** Run the four focused test files and typecheck the affected workspaces. Commit as `fix(logs): preserve complete activity target details`.

### Task 2: Add per-row expand/collapse and lossless copy/export

**Files:**

- Add: `apps/desktop/src/renderer/features/logs/ExpandableTargetDetail.tsx`
- Modify: `apps/desktop/src/renderer/features/worklog/WorkLogPanel.tsx`
- Modify: `apps/desktop/src/renderer/features/live/LogStreamPanel.tsx`
- Modify: `apps/desktop/src/renderer/features/live/StandaloneLogViewer.tsx`
- Modify: `apps/desktop/src/main/main.ts`
- Modify: `apps/desktop/src/main/desktop-services.ts`
- Modify: `packages/storage/src/audit-repository.ts`
- Modify: `packages/ipc-contracts/src/index.ts`
- Modify: `apps/desktop/src/preload/index.ts`
- Modify: `apps/desktop/src/renderer/i18n/messages.ts`
- Modify: `apps/desktop/src/renderer/styles.css`
- Test: `apps/desktop/tests/worklog-panel.test.ts`
- Test: `apps/desktop/tests/live-logs-layout.test.ts`
- Add: `apps/desktop/tests/log-detail-export.test.ts`

**RED:** Assert a long row starts collapsed, has localized “ดูเพิ่ม/Show more”, toggles `aria-expanded`, lazy-fetches only that detail and exposes all items after click, and “แสดงน้อยลง/Show less” collapses only that row. Assert copy and exported `.txt` contain all 7/9 items while collapsed. Cover persisted and in-flight row IDs, a call that completes between click and export, hidden-item search, evicted detail handling, and an old `(+N)` row with the legacy-loss warning.

**Implementation:** Render a short heading and semantic list after a bounded one-row lazy fetch; keep expansion state keyed per stable row identity. Work Log export captures ordered `audit:<eventId>` / `inflight:<callId>` references at click and main resolves in-flight-to-started-audit races. Live Logs sends line IDs plus correlation refs. A shared main formatter streams all resolved items to the destination file; search runs against main-process/SQLite detail and returns matching IDs only. Keep event retention limits unchanged and document them separately from per-event completeness.

**GREEN:** Run renderer unit tests, IPC contract tests, and a Playwright keyboard/visual smoke at the supported desktop viewport. Run the Impeccable detector once on the changed UI targets. Commit as `fix(desktop): add complete expandable log details`.

---

## Phase 2 — Truthful Tool Readiness and Usable Status UI

### Task 3: Separate readiness reason, delivery state, and Browser runtime state

**Files:**

- Modify: `packages/ipc-contracts/src/index.ts`
- Modify: `apps/desktop/src/preload/index.ts`
- Modify: `apps/desktop/src/main/tool-catalog/tool-catalog-service.ts`
- Modify: `apps/desktop/src/main/tool-catalog/catalog-definitions.ts`
- Modify: `apps/desktop/src/main/tool-catalog/external-tool-catalog-adapter.ts`
- Modify: `apps/desktop/src/main/tool-catalog/remediation-registry.ts`
- Modify: `apps/desktop/src/renderer/features/tools/ToolsPage.tsx`
- Modify: `apps/desktop/src/renderer/features/tools/ToolDetailModal.tsx`
- Modify: `apps/desktop/src/renderer/i18n/messages.ts`
- Test: `apps/desktop/tests/tool-catalog-readiness.test.ts`
- Test: `apps/desktop/tests/tools-doctor-ui.test.ts`
- Test: `apps/desktop/tests/external-tool-catalog-adapter.test.ts`

**RED:** Cover `runtime_not_ready`, `probe_failed`, `permission_denied`, `unsupported_platform`, `feature_disabled`, `blocked_by_safety_policy`, `planned`, and `external_unknown`. Assert a stopped Managed Browser says “ต้องเปิดใช้งาน”, retains `available=true`, and offers “เปิด Managed Browser”; only actual setup gaps say “ต้องตั้งค่า”. Assert UNKNOWN is reserved for inconclusive probes.

**Implementation:** Add optional backward-compatible `readinessReason` and `deliveryState` fields. Keep the coarse readiness enum for filters/API compatibility but derive the card/modal label and remediation from the reason. Expose failed composite requirements for Accessibility/Computer Use/UI Target Action so users see exactly which bridge/input/vision permission is missing.

**GREEN:** Run catalog/preload/UI tests in both Thai and English. Commit as `fix(tools): report actionable readiness reasons`.

### Task 4: Repair selected status contrast and control semantics

**Files:**

- Modify: `apps/desktop/src/renderer/features/tools/ToolsPage.tsx`
- Modify: `apps/desktop/src/renderer/styles.css`
- Test: `apps/desktop/tests/tools-doctor-ui.test.ts`
- Test: `apps/desktop/e2e/tools-doctor.spec.ts`

**RED:** Assert each status filter is a toggle with `aria-pressed`; selected number and label inherit the dark active foreground; keyboard focus remains visible. Add computed-color acceptance requiring WCAG AA rather than snapshotting class names only.

**Implementation:** Scope muted label rules to inactive cards, set active descendants to `color: currentColor`, preserve gold selection with dark high-contrast foreground, and add a non-color selected indicator/focus outline.

**GREEN:** Run unit, Electron E2E, 100%/125% Windows scale smoke, and Impeccable detector. Commit with Task 3 if the diff is inseparable; otherwise `fix(desktop): improve tool status contrast`.

### Task 5: Make Windows capability bridge packaging deterministic

**Files:**

- Modify: `apps/desktop/scripts/write-capability-integrity.mjs`
- Modify: `apps/desktop/electron-builder.yml`
- Modify: `apps/desktop/scripts/capture-packaged-runtime-evidence.mjs`
- Modify: `apps/desktop/scripts/write-release-evidence.mjs`
- Modify: `apps/desktop/scripts/verify-release-evidence.mjs`
- Add: `apps/desktop/scripts/verify-capability-bridge-artifacts.mjs`
- Add: `tests/packaging/capability-bridge-packaging.test.ts`
- Modify: `tests/packaging/desktop-packaging.test.ts`
- Modify: `tests/packaging/windows-trust-evidence.test.ts`
- Modify: `apps/desktop/src/main/capability-runtime.ts`
- Add: `tests/packaging/packaged-capability-runtime.test.ts`

**RED:** Prove LF and CRLF buffers retain exact bytes; mutate source after staging and assert packaging still consumes staged bytes; fail closed for malformed manifest, byte-count mismatch, digest mismatch and app-embedded expectation mismatch.

**Implementation:** Stage the raw source `Buffer` once under `apps/desktop/build/capability-bridge/`; derive compiled constant, `.sha256` and descriptor from those bytes. electron-builder copies only staged artifacts. Post-pack verification extracts the actual `dist/main/main.js` entry from `app.asar` and requires source/staged/manifest/compiled expectation/win-unpacked equality. Add the bridge identity to provenance and checksums. Verify Setup and Portable extraction independently, then launch unpacked/portable with an isolated user-data directory and invoke the real Accessibility/Computer Use/UI Target requirement probes before release evidence is accepted.

**GREEN:** Run root `tests/packaging`, desktop build, unpacked verifier, isolated packaged-runtime probe and Windows package smoke. Confirm Accessibility, Computer Use and UI Target requirements report their real backend/actionable subrequirements instead of integrity UNKNOWN. Commit as `fix(packaging): bind capability bridge bytes to artifacts`.

---

## Phase 3 — Operational and Governed `agent_swarm_run`

### Fixed public MCP contract

The dedicated tool uses a strict discriminated union; unknown keys are rejected. `workspaceId` reuses the repository `WorkspaceIdSchema`, `swarmId` is a UUID generated by the service, and every operation is owner-scoped from the trusted MCP actor rather than an input owner field.

| Operation | Exact request fields and bounds | Exact bounded response |
| --- | --- | --- |
| `start` | `{ operation:'start', workspaceId, idempotencyKey, accessMode:'read_only', tasks, maxConcurrency? }`; `idempotencyKey` UUID; `tasks` 1–4 unique entries; `maxConcurrency` integer 1–4 and no greater than task count, default 2 | `AgentSwarmSnapshot` with at most 4 task snapshots; retrying the same owner/workspace/key returns the same swarm and never launches extra quota work |
| start task | `{ id, prompt, dependsOn? }`; `id` matches `^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$`; `prompt` nonblank and at most 32 KiB UTF-8; `dependsOn` at most 3 unique task IDs, cannot self-reference, all IDs must exist, whole graph must be acyclic | Prompt is accepted for launch but never returned or persisted; snapshot exposes only task ID, dependency IDs, state, timestamps, result availability and a redacted error no longer than 2,048 chars |
| `status` | `{ operation:'status', workspaceId, swarmId }` | One `AgentSwarmSnapshot`; task array maximum 4; no prompt, OS PID, command line or cross-owner information |
| `result` | `{ operation:'result', workspaceId, swarmId, taskId, cursor?, maxBytes? }`; `taskId` uses the task-ID regex; opaque cursor max 128 chars; `maxBytes` integer 1–16,384, default 8,192 | `{ swarmId, taskId, state, text, nextCursor?, eof }`; `text` at most requested UTF-8 bytes and output is redacted before persistence/return |
| `cancel` | `{ operation:'cancel', workspaceId, swarmId }` | Owner-scoped snapshot with one terminal/cancel outcome per task; repeated cancel is idempotent and never targets an unverified/restarted PID |
| `list` | `{ operation:'list', workspaceId, cursor?, limit? }`; opaque cursor max 128 chars; `limit` integer 1–50, default 20 | `{ items, nextCursor? }`; at most 50 owner-visible swarm summaries, each without task output/prompt/process identity |

`AgentSwarmSnapshot.state` is exactly `queued | running | completed | failed | cancelled | termination_unverified`; task state is exactly `blocked | queued | running | completed | failed | cancelled | termination_unverified`. Timestamps are ISO-8601 strings, error strings are redacted/bounded, result pages never split invalid UTF-8, and total stored output per task is capped at 1 MiB with an explicit `outputTruncated` flag. Reaching that storage cap does not hide task identity/state and is distinct from log-item completeness elsewhere in this plan.

### Task 6: Add enforceable Codex read-only mode and owned result spooling

**Files:**

- Modify: `packages/codex/src/codex-capabilities.ts`
- Modify: `packages/codex/src/codex-adapter.ts`
- Modify: `packages/application/src/codex-service.ts`
- Modify: `packages/process/src/process-manager.ts`
- Modify: `packages/process/src/index.ts`
- Test: `packages/codex/src/codex-sandbox.test.ts`
- Test: `packages/application/src/codex-service.test.ts`
- Add: `packages/process/src/owned-process-output.test.ts`

**RED:** Assert read-only mode produces supported CLI argv or fails closed, swarm requests cannot select workspace-write, handles/output are owner-scoped, terminal output survives service reconstruction, and an active pre-restart process becomes `termination_unverified`. Assert cancellation cannot target another owner’s process or a PID without a verified live handle.

**Implementation:** Discover supported Codex sandbox modes from the installed CLI contract and add an explicit read-only launch path. Spool owned stdout/stderr and terminal receipt to bounded durable files for swarm children while keeping live cancellation tied to the verified in-process handle. Persist minimal identity/ownership without prompt plaintext; use `termination_unverified` after restart when an active handle cannot be proven instead of attempting PID-only termination.

**GREEN:** Run Codex/application/process unit and integration suites without launching billable provider calls. Commit as `feat(codex): add governed durable task substrate`.

### Task 7: Build the Agent Swarm deep module and durable ledger

**Files:**

- Add: `packages/application/src/agent-swarm-types.ts`
- Add: `packages/application/src/agent-swarm-service.ts`
- Modify: `packages/application/src/index.ts`
- Add: `packages/storage/src/agent-swarm-repository.ts`
- Add: `packages/storage/src/migrations/agent-swarm-migration.ts`
- Modify: `packages/storage/src/database.ts`
- Modify: `packages/storage/src/index.ts`
- Test: `packages/application/src/agent-swarm-service.test.ts`
- Add: `packages/storage/src/agent-swarm-repository.integration.test.ts`

**RED:** Cover 1–4 task validation, default/max concurrency, dependency DAG cycles, concurrent read-only admission, rejection of every write mode/claim, partial-launch rollback, idempotent cancellation, owner isolation, prompt non-persistence, terminal CAS/result recovery and active-restart `termination_unverified` reconciliation.

**Implementation:** Implement `start/status/result/cancel/list` behind one service with exact bounded request/result types. Store swarm/task state, prompt digest/length, owned live handles and durable terminal output refs. Allow concurrent read-only tasks up to the per-swarm limit and reject all write access in v4.44.0. Return per-child results and cancellation outcomes; never report atomic success after a partial failure.

**GREEN:** Run application/storage suites including two-service-instance contention. Commit as `feat(agent-swarm): add durable bounded orchestration`.

### Task 8: Expose Agent Swarm through MCP policy, runtime wiring, and catalog

**Files:**

- Add: `packages/mcp-server/src/tools/agent-swarm-tools.ts`
- Modify: `packages/mcp-server/src/tools/tool-types.ts`
- Modify: `packages/mcp-server/src/tools/upgrade-tools.ts`
- Modify: `packages/mcp-server/src/upgrade-catalog.ts`
- Modify: `packages/mcp-server/src/upgrade-runtime.ts`
- Modify: `packages/mcp-server/src/tool-registry.ts`
- Modify: `packages/mcp-server/src/mutation-policy.ts`
- Modify: `apps/cli/src/runtime/stdio-mcp-runtime.ts`
- Modify: `apps/desktop/src/main/desktop-services.ts`
- Modify: `apps/desktop/src/main/tool-catalog/catalog-definitions.ts`
- Modify: `apps/desktop/src/main/tool-catalog/tool-catalog-service.ts`
- Modify: `apps/desktop/src/main/tool-catalog/remediation-registry.ts`
- Test: `packages/mcp-server/src/tool-registry.test.ts`
- Test: `packages/mcp-server/src/mutation-policy.test.ts`
- Test: `packages/mcp-server/src/host-approval-boundary.test.ts`
- Test: `packages/mcp-server/src/scheduled-continuation-fence.test.ts`
- Test: `packages/mcp-server/src/tool-runtime-contract.test.ts`
- Test: `apps/desktop/tests/tool-catalog-readiness.test.ts`

**RED:** Assert the tool is absent when Codex opt-in is off, present and dependency-ready when on, is removed from the generic upgrade passthrough, validates bounded discriminated Zod schemas/outputs for `start|status|result|cancel|list`, exact-action approval includes workspace and launch count, `start/cancel` are opaque mutation while `status/result/list` are read, every write request is rejected, and no quota call occurs in automated tests.

**Implementation:** Remove/filter `agent_swarm_run` from generic `upgradeTools`, add the dedicated service to `McpApplicationServices`, and retain one public name with exact operation schemas and bounded result pages. Add explicit Codex-quota classification independent of a `codex_` prefix. Trusted authorization comes from the registry/host, not a boolean input. Advertise only when dependency and policy gates pass; otherwise show a specific opt-in/safety reason. Update generator/runtime fixtures and catalog counts from evidence (expected total remains 231, default remains 195, Codex-enabled becomes 202 if no other registry change).

**GREEN:** Run registry/policy/runtime/catalog suites. Perform at most one manually authorized, low-impact read-only real-provider smoke in a disposable Git fixture and record it separately; automated tests must use fakes. Commit as `feat(mcp): deliver policy-gated agent swarm tool`.

---

## Phase 4 — Deterministic Gates, Version 4.44.0, and Local Installer

### Task 9: Make the Windows Sandbox runtime-contract fixture deterministic

**Files:**

- Modify: `packages/mcp-server/src/tool-runtime-contract.test.ts`
- Modify only if a production defect is demonstrated: `packages/mcp-server/src/sandbox-runtime.ts`

**RED:** Reproduce both host cases with injected availability fixtures: Windows Sandbox absent and present. Assert declared evidence is truthful in both cases and does not depend on the developer machine.

**Implementation:** Isolate the declared-unavailable fixture from live host discovery. Keep a separate host-conditional integration check rather than weakening production behavior.

**GREEN:** Run the focused runtime-contract suite twice with deterministic available/unavailable fakes. Commit as `test(mcp): isolate sandbox runtime evidence fixtures`.

### Task 10: Synchronize version, release notes, contracts, and operator guidance

**Files:**

- Modify via version tool: all workspace `package.json` files and generated `APP_VERSION`/version assertions
- Modify: `README.md`
- Modify: `docs/architecture/MULTI_WORKSPACE_CONCURRENCY.md`
- Modify: `docs/architecture/TOOL_CONTRACT.md`
- Modify: `docs/USAGE_TH.md`
- Modify: `.github/RELEASE_CHECKLIST.md`
- Modify generated tool catalog/docs through the repository generator

**Steps:** Run `corepack pnpm@10.15.0 run set-version 4.44.0`, then document only verified changes. Explain Browser start-vs-setup, Computer Use requirements, Agent Swarm opt-in/policy, log expansion/export and legacy `(+N)` limits. Regenerate counts and reject stale 4.31.0 release headings.

**GREEN:** Search tracked files for stale current-version/count claims, run docs/tool-catalog checks and `git diff --check`. Commit as `chore(release): prepare v4.44.0 local test build`.

### Task 11: Full verification, artifact evidence, commit audit, push `dev`

**Required gates:**

```powershell
corepack pnpm@10.15.0 install --frozen-lockfile
corepack pnpm@10.15.0 test:packaging
powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\verify-release.ps1
node apps/desktop/scripts/verify-capability-bridge-artifacts.mjs
node apps/desktop/scripts/verify-release-evidence.mjs
git diff --check
git status --short --branch
```

**Artifact acceptance:** Verify, without installing, these local outputs under `apps/desktop/dist/installers/`:

- `lnwjud-Setup-4.44.0.exe`
- `lnwjud-Setup-4.44.0.exe.blockmap`
- `lnwjud-Portable-4.44.0.exe`
- `latest.yml`
- `portable.yml`
- `SHA256SUMS.txt`
- `PROVENANCE.json`

Record SHA-256, size, source commit, bridge digest/byte count, Setup/Portable extracted bridge verification, signing state, and any clean-VM/manual prerequisites separately. `makeappx.exe`/`signtool.exe` absence does not block unsigned local Setup/Portable but must be reported and must not be called signed.

**Review and Git:** Review every task diff plus cross-task integration, ensure no secrets/build outputs are staged, and confirm all scoped commits are based on the same verified tree. Push exactly `git push origin dev`. Re-read remote `origin/dev` after push. Do not tag, publish, create a Release, merge to `main`, or install the artifacts.

---

## Completion Contract

Work is complete only when all of the following are true:

- New file/tool collection events retain and export every structured item; UI collapse never changes stored/exported content
- Thai/English expand controls work per row with keyboard and screen-reader state
- Selected Tools status cards have verified readable label/number/focus contrast
- Managed Browser stopped state is “ต้องเปิดใช้งาน”, not “ต้องตั้งค่า”
- Accessibility, Computer Use and UI Target Action packaged probes are backed by verified bridge bytes and show exact actionable requirements
- `agent_swarm_run` is absent without Codex opt-in and operational in bounded, authorized, read-only mode when enabled; terminal results are durable and unverifiable active jobs after restart are reported as `termination_unverified`
- All focused and full release gates pass, or any genuine blocker is reported without a false completion claim
- Local 4.44.0 Setup and Portable artifacts plus provenance/checksums exist and pass artifact verification
- Reviewed source commits are pushed to `origin/dev`; no tag/publish/release/install action occurred
